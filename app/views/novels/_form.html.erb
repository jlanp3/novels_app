<% if @novel.new_record? %>
	<h1>New Novel</h1>
<% else %>
	<h1>Edit novel name</h1>
<% end %>

<%= form_for @novel do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name  %>

	<%= @novel.new_record? ? (f.submit) : (f.submit 'Edit Name') %>

<% end %>